import React from "react";
import ReactDOM from "react-dom/client";
import NoblDogTracker from "./NoblDogTracker.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NoblDogTracker />
  </React.StrictMode>
);
